{
  "name": "fleet-pivot-bun-convex",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "sync:export": "bun src/sync/convexTrackSync.ts export",
    "sync:import": "bun src/sync/convexTrackSync.ts import",
    "migrate:sqlite": "bun src/migration/importSqlite.ts",
    "worker:demo": "bun src/worker/runDemo.ts"
  },
  "dependencies": {
    "convex": "^1.34.1"
  },
  "devDependencies": {
    "bun-types": "^1.3.10",
    "typescript": "^5.9.3"
  }
}
