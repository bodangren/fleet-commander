# God-file allowlist for doctor.sh (Check 4).
#
# Source files in frontend/src, pivot/src, and convex are expected to stay under
# the line threshold (see GODFILE_THRESHOLD in doctor.sh). Files listed here are
# KNOWN pre-existing god-files tracked as tech debt to be split over time; they
# do not fail the check. New files that exceed the threshold do.
#
# Each line is a path relative to the repo root. Blank lines and `#` lines ignored.

pivot/src/orchestrator/orchestrator.ts   # TD-206 — 1034-line runProject god-function
frontend/src/hooks/useAgentForm.ts       # 633 lines — agent editor form state
convex/notifications.ts                  # 611 lines — notification CRUD + emitters
convex/fleetCatalog.ts                   # 551 lines — harness/catalog CRUD
frontend/src/pages/OptimizePage.tsx      # 502 lines — A/B + policy tuning page
