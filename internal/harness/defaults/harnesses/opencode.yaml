name: Opencode
binary: opencode
discovery:
  command: "opencode --list-models"
  parse_strategy: line-per-model
  pattern: ""
invocation:
  template: "opencode --model {model} --prompt {prompt}"
  flags:
    no_interactive: "--no-interactive"
