# Review: E2E Test Baseline Hardening — 2026-06-19

## 1. Summary of changes

This track was created in the last 24 hours by commit `6accd94`. The commit added the standard track scaffolding:

- `measure/tracks/e2e_test_baseline_hardening_20260619/metadata.json`
- `measure/tracks/e2e_test_baseline_hardening_20260619/spec.md`
- `measure/tracks/e2e_test_baseline_hardening_20260619/plan.md`
- Updated `measure/tracks.md` to list the track under the planned review section.

No implementation work, tests, or source-code changes were committed.

## 2. Spec alignment

**Not started / Missed.**

None of the acceptance criteria have been addressed yet:

- AC 1–5 and 7 (Playwright baseline green, deterministic seeds, shared factory, quality-gate wiring, CI failure on regression) are unimplemented.
- AC 6 (critical-path smoke spec) is unimplemented.

This is expected for a newly created track, but the review flags it so the next cycle does not assume progress.

## 3. Code quality observations

- N/A — only documentation scaffolding exists.
- The spec and plan are well-structured: clear phases, bounded scope (harden existing suite rather than expand coverage), and explicit non-goals.
- `metadata.json` correctly references predecessor tracks (`e2e_audit_remediation_20260603`, `e2e_qa_smoke_20260613`).

## 4. Risks or blockers

- **No immediate risk** — the track is intentionally at the starting line.
- **Dependency risk:** The plan depends on prior E2E audit/smoke tracks. Verify those predecessors are complete before beginning Phase 1.
- **Scope creep watch:** The spec explicitly avoids adding large amounts of new coverage and avoids replacing Playwright. Future commits should respect these non-goals.

## 5. Recommended next actions

1. Run `npx playwright test` on a clean worktree to capture the current baseline failure report (Phase 1, Task 1).
2. Add the `scripts/e2e-baseline-audit.test.ts` contract test that asserts the audit report shape and known-failure IDs (Phase 1, Task 2).
3. Classify failures and update `measure/tech-debt.md` to remove or reclassify TD-250 (Phase 1, Task 4).
4. Begin Phase 2 by designing the shared seed fixture schema in `e2e/helpers/seed.ts`.
