# Daily Review — e2e_test_baseline_hardening_20260619

## Summary of changes

The last 24 hours did not add new source, test, or implementation changes to this track. The only change was the addition of the prior day's review artifact:

- `measure/tracks/e2e_test_baseline_hardening_20260619/review-2026-06-22.md`

The track remains in the same state documented in that review: Phases 1–6 are complete, and Phase 4 Task 7 (full E2E baseline green on a clean checkout) remains `[~]` with 32 classified failures.

## Spec alignment

**Partial.**

- AC 2 (deterministic seed fixture for every canonical flow) is **met** by the `frontend/e2e/helpers/seed.ts` factory and migration of all 27 specs.
- AC 3 (remove/replace mock/data-seeding anti-patterns) is **partially met** — 28 adapter-mock-drift failures were classified and handed off to TD-250-adapter.
- AC 4 (E2E baseline command documented and invoked by quality workflow) is **met** — `measure/doctor.sh` has a bounded `e2e` subcommand with `--dry-run` and `QUALITY_PROFILE` awareness, and `measure/tech-stack.md` documents the canonical command.
- AC 5 (no flaky tests remain in baseline) is **partially met** — no `@quarantine` markers exist, but 11 race-classified failures remain unaddressed.
- AC 6 (smoke spec proves critical path) is **met** — `frontend/e2e/smoke.spec.ts` uses the seed factory and exercises dashboard/project/kanban loading.
- AC 7 (CI/verify gate fails on regression) is **met** for the bounded smoke gate wired into `doctor.sh all`.
- AC 1 (`npx playwright test` green on clean checkout) is **not yet met** — the latest baseline shows 35 pass / 32 fail.

## Code quality observations

- The Phase 4 SHAPE gate (`measure/tests/e2e-doctor-wiring.test.sh`) is well-designed: it proves command construction, profile awareness, and bounded argv without ever invoking the full Playwright suite.
- Seed factory and spec-stability contract tests are thorough and run quickly.
- The failure taxonomy is clear and mapped to tech-debt IDs.

## Risks or blockers

- **Task 7 remains deferred.** The full E2E suite cannot be trusted as a quality gate until it is green.
- **Playwright version collision:** root `node_modules` has v1.59.1 while `frontend/node_modules` has v1.60.0; `npx playwright` picks the stale root binary. The canonical run should use `frontend/node_modules/.bin/playwright`.
- **Selector drift** across 23+ specs references UI text/buttons that no longer exist in the current frontend.
- **Convex-only dashboard specs** cannot be satisfied by the mock adapter and may need a different testing strategy.

## Recommended next actions

1. Re-run the full Playwright suite using `frontend/node_modules/.bin/playwright` and capture an updated `baseline.json`.
2. Resolve or quarantine the remaining 32 failures using the Phase 1 taxonomy (18 adapter-mock-drift, 1 selector-drift, 11 race, 2 genuine-regression).
3. Update `measure/tech-stack.md` to document the correct Playwright binary path if the version collision persists.
4. Update `measure/tech-debt.md` with any newly classified items.
5. Flip Phase 4 Task 7 to `[x]` only when the full suite is green on a clean checkout, then archive the track.
