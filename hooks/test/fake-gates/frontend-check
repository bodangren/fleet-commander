#!/usr/bin/env bash
# Fake gate stub: frontend-check
# Real command: `bun --cwd frontend check`
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$SCRIPT_DIR/_lib.sh"
run_fake_gate frontend-check "$@"
