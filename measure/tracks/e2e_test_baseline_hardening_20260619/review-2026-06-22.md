# Daily Review — e2e_test_baseline_hardening_20260619

## Summary of changes

The last 24h touched the E2E quality-gate surface as part of the build-graph acceptance closeout:

- `measure/doctor.sh` gained a bounded `e2e` subcommand with `--dry-run` support and `QUALITY_PROFILE` awareness.
- `measure/tech-stack.md` documents the canonical `npx playwright test` command and required Vite env vars.
- `measure/tests/e2e-doctor-wiring.test.sh` verifies the gate is bounded to `frontend/e2e/smoke.spec.ts` and not silently run by `doctor.sh all`.

The track’s seed factory, critical-path spec stabilization, and baseline classification were already committed in prior work. Phase 4 Task 7 (full E2E baseline green on a clean checkout) remains `[~]` with 32 classified failures.

## Spec alignment

**Partial.**

- AC 2–6 met: deterministic seed factory, shared fixture, anti-patterns classified, smoke spec exists, no flaky tests quarantined.
- AC 4 met: E2E baseline command is documented and wired into `doctor.sh`.
- AC 7 met: the `verify` path will fail if the bounded smoke spec regresses.
- AC 1 **not yet met**: the full Playwright suite still reports 32 failures on a clean checkout (18 adapter-mock-drift, 1 selector-drift, 11 race, 2 genuine-regression).

## Code quality observations

- The bounded `--dry-run` gate is well designed: it prints the exact argv, refuses silent fall-through, and never invokes the full suite in the contract test.
- Seed factory and spec-stability contract tests are thorough and run quickly.
- The full-suite failure taxonomy is clear and mapped to tech-debt IDs (TD-250-adapter, TD-256-selector, TD-257-race, TD-259-regression).

## Risks or blockers

- Task 7 has been deferred across multiple attempts. The full E2E suite cannot be trusted as a quality gate until it is green.
- A `@playwright/test` version collision exists between root `node_modules` (v1.59.1) and `frontend/node_modules` (v1.60.0); `npx playwright` picks the stale root binary.
- Several E2E specs reference UI text/buttons that no longer exist (selector-drift), requiring either spec updates or explicit quarantine.

## Recommended next actions

1. Resolve or quarantine the 32 remaining failures using the Phase 1 taxonomy.
2. Use `frontend/node_modules/.bin/playwright` (v1.60.0) for the canonical full-suite run and update `tech-stack.md` if necessary.
3. Update `baseline.json` and `tech-debt.md` after the full run.
4. Flip Phase 4 Task 7 to `[x]` only when `npx playwright test` is green on a clean checkout.
