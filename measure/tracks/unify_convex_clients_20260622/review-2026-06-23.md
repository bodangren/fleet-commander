# Daily Review — unify_convex_clients_20260622

## Summary of changes

The last 24 hours did not change any source or test files for this track. The only changes were the track scaffolding (created during the 2026-06-22 daily closeout):

- `measure/tracks/unify_convex_clients_20260622/metadata.json`
- `measure/tracks/unify_convex_clients_20260622/spec.md`
- `measure/tracks/unify_convex_clients_20260622/plan.md`

All plan tasks remain `[ ]` and no code has been written. The track is ready for Phase 1 Red work.

## Spec alignment

**Not started.**

- AC 1–7 are all pending implementation.
- The plan correctly structures the work as TDD: prove drift with Red contract/import tests, merge implementations, migrate imports, remove deprecated module, and verify gates.

## Code quality observations

- N/A — no code changes yet.

## Risks or blockers

- This is a critical-debt track (TD-204). The two client modules (`pivot/src/convexClient.ts` and `pivot/src/typedConvexClient.ts`) continue to diverge, causing inconsistent error handling, duplicated helpers, and confusion over the correct import.
- Import surface across `pivot/src/` is likely large; a mechanical migration is needed to avoid partial refactors that leave both modules in use.
- The plan must avoid introducing new `as any` escapes during the migration, and the canonical client must preserve mock-client injection and environment-selection behavior.

## Recommended next actions

1. **Phase 1 Red:** Create `pivot/src/convexClient.unify.test.ts` with failing tests that:
   - assert only one canonical client module exists,
   - list all callers of the deprecated client path,
   - assert the canonical client preserves mock injection and error handling.
2. Run the targeted Red command and record the failure count in `plan.md`.
3. **Phase 2 Green:** Choose the canonical module (recommended: `pivot/src/typedConvexClient.ts`), merge the other implementation's behavior into it, and migrate all `pivot/src/` imports.
4. **Phase 3:** Delete the deprecated module once all imports are migrated.
5. Run `bun --cwd pivot test` and `bun --cwd pivot typecheck`.
6. Run `build-graph update ./graph.db <changed files>` after source edits.
7. Mark TD-204 resolved in `measure/tech-debt.md`.
