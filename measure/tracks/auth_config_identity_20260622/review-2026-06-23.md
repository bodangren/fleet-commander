# Daily Review — auth_config_identity_20260622

## Summary of changes

The last 24 hours advanced Phase 1 (Red) of the Auth Config and Identity track:

- Created shared test fixtures in `convex/__fixtures__/auth.ts` (`createCtx`, `withNodeEnv`, `withAuthEnv`, `loadAuthConfig`) with proper env restoration in `afterEach`.
- Added `convex/auth.config.test.ts` with two contract tests:
  - provider config loads from env vars in production.
  - loading rejects localhost defaults in production when env vars are unset.
- Extended `convex/lib/auth.test.ts` to require `FLEET_ALLOW_ANON_BOOTSTRAP=1` for anonymous bootstrap even in development.
- Updated `measure/tracks/auth_config_identity_20260622/plan.md` to mark Phase 1 tasks `[~]` and recorded the Red failure count.
- Added `measure/tracks/auth_config_identity_20260622/test-strategy.md` documenting the tightened contract and the stale premise that `auth.config.ts` already exists at HEAD.

The targeted Red command was executed live:

```bash
bun test ./convex/lib/auth.test.ts ./convex/auth.config.test.ts
```

Result: **4 pass, 2 fail** — the two new tightened-contract assertions fail because `auth.config.ts` does not yet reject missing provider env vars and `resolveActor` still allows anonymous bootstrap in development without the opt-in flag.

## Spec alignment

**Partial.**

- AC 5 (Red tests fail at HEAD because auth config is missing/wrong) is **met for the tightened contract**: two genuine assertion failures prove the gap.
- AC 1 (`auth.config.ts` exists and exports valid provider configuration) is **partially met** — the file exists but currently silently accepts localhost defaults in production.
- AC 2 (`resolveActor` returns real identity for valid token) is **met** by existing behavior and covered by tests.
- AC 3 (anonymous bootstrap removed for production; dev opt-in remains) is **not yet met** — anonymous is still allowed in any non-production environment without an explicit flag.
- AC 4 (unauthenticated requests fail with clear error) is **met for production** but not yet gated behind the explicit dev opt-in.
- AC 6–7 (test suite and typechecks green after fix) are **pending** Phase 2/3 implementation.

## Code quality observations

- Shared auth fixtures reduce duplication and properly restore `NODE_ENV`, `FLEET_ALLOW_ANON_BOOTSTRAP`, `CONVEX_AUTH_PROVIDER_DOMAIN`, and `CONVEX_AUTH_APPLICATION_ID` after each test.
- `loadAuthConfig` uses a cache-busting query string so provider env vars are re-evaluated per dynamic import — important because `auth.config.ts` is evaluated at module load time.
- The `createCtx` helper casts the mock context as `any`, which is acceptable for a unit-test fixture but should not leak into production code.
- The `test-strategy.md` correctly flags that the original spec and TD-201 premise (`auth.config.ts` missing) is stale, and it proposes a stronger contract rather than a vacuous Red.
- Minor plan hygiene issue: `plan.md` still references the non-existent `bun test ./convex/auth.test.ts` in its introductory commands. The actual targeted command used in Phase 1 is the correct two-file form.

## Risks or blockers

- **Stale debt description:** TD-201 currently claims `auth.config.ts` is missing, but the file exists. The closeout must rewrite the TD entry to describe the real gap (localhost defaults allowed in production, anonymous fallback not explicitly opt-in).
- **Blast radius:** `resolveActor` is imported by 20+ Convex modules; changing the dev fallback to require an explicit env var may break local development or existing tests that rely on implicit anonymous actors. A dev-only opt-in must be clearly documented.
- **CI risk:** If the repo-level test runner ever sweeps `convex/`, the intentional Red tests could fail CI until Phase 2 is completed. The current `npm test` alias only runs `pivot` tests, so the risk is contained.

## Recommended next actions

1. **Phase 2 Green:** Update `convex/auth.config.ts` to throw when `CONVEX_AUTH_PROVIDER_DOMAIN` or `CONVEX_AUTH_APPLICATION_ID` are unset under `NODE_ENV=production`.
2. **Phase 2 Green:** Gate anonymous bootstrap in `convex/lib/auth.ts` behind `process.env.FLEET_ALLOW_ANON_BOOTSTRAP === '1'` regardless of `NODE_ENV`.
3. Re-run `bun test ./convex/lib/auth.test.ts ./convex/auth.config.test.ts` and confirm 0 failures.
4. **Phase 3:** Add an integration test that invokes a protected Convex mutation/query with an unauthenticated mock context under `NODE_ENV=production` and asserts rejection.
5. Run `bun --cwd pivot typecheck` and `bun --cwd frontend typecheck`; keep them green.
6. Run `build-graph update ./graph.db convex/auth.config.ts convex/lib/auth.ts` after source edits.
7. Rewrite the TD-201 entry in `measure/tech-debt.md` to reflect the real gap and mark resolved.
8. Fix the plan's Red/Green commands to use the two-file form that matches `test-strategy.md`.
