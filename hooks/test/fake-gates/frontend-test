#!/usr/bin/env bash
# Fake gate stub: frontend-test
# Real command: `bun --cwd frontend test`
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$SCRIPT_DIR/_lib.sh"
run_fake_gate frontend-test "$@"
