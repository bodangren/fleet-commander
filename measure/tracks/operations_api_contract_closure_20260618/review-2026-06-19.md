# Review: Operations API Contract Closure — 2026-06-19

## 1. Summary of changes

Over the last 24 hours the track landed 8 commits spanning Phase 1–3:

- **Phase 1 — Contract inventory (Red):** Added `convex/pipelines.test.ts` to pin placeholder behavior, extended `pivot/src/routes/pipelines.test.ts` with `GET /api/pipelines` cases, added `pivot/src/routes/reconciliation.test.ts`, and extended `frontend/src/__tests__/router-inventory.test.ts` with a pivot route inventory that cross-checks every frontend `/api/reconciliation` and `/api/pipelines` fetch against registered routes.
- **Phase 2 — Reconciliation routes (Green):** Created `pivot/src/routes/reconciliation.ts` with `GET /api/reconciliation/proposals`, `POST .../apply`, and `POST .../reject`, registered them in `pivot/src/server.ts`, and added `GET /api/pipelines` wired to `api.pipelineRuns.listPipelineRunsHandler`.
- **Phase 3 — Pipeline persistence (Red, in progress):** Extended `convex/pipelines.test.ts` and `pivot/src/routes/pipelines.test.ts` with inversion tests that fail while `convex/pipelines.ts` still exports placeholder functions (`startPipeline`, `updatePipelineStatus`, `getPipelineLogs`) and returns `'stub-id'`.

## 2. Spec alignment

**Partial.**

- **Met (AC 1–5, 9):** Reconciliation list/apply/reject routes exist and are registered; `GET /api/pipelines` is implemented and covered by route tests; the route-inventory contract test cross-checks frontend fetches.
- **Partial (AC 6, 7):** `GET /api/pipelines` uses real `pipelineRuns` persistence, but `convex/pipelines.ts` still contains the placeholder `startPipeline`, `updatePipelineStatus`, and `getPipelineLogs` exports. Phase 3 Red tests are in place to drive their removal/delegation, but the Green commit has not landed yet.
- **Not started (AC 8):** Frontend page-level tests for `ReconcilePage`, `PipelinesPage`, and `usePipelineData` production response/error shapes are still listed as Phase 4 tasks and have not been added.

## 3. Code quality observations

- Clean separation of concerns: new reconciliation routes reuse existing Convex proposal functions via `api.reconciliationProposals.listPendingProposals` / `resolveProposal`.
- Tests use a mocked Convex client, making them fast and deterministic while still exercising the full route handler path.
- Good Red-phase documentation: explicit fail counts and notes about which tests are placeholder-regression vs. inversion.
- `metadata.json` is currently stale — all sprint stories are `pending` even though S1 and S2 are functionally complete.

## 4. Risks or blockers

- **Placeholder code still in production:** `convex/pipelines.ts` returns `null`/`[]`/`'stub-id'` for live pipeline operations. Until Phase 3 Green lands, the Pipelines page persistence is only partially real.
- **Story status mismatch:** `metadata.json` does not reflect Phase 1/2 completion, which can mislead dashboards and the next automation cycle.
- **Graph.db sync deferred:** The plan notes graph.db updates are owned by the closeout role; ensure this is not forgotten when the track wraps.
- **No severe blockers** for continued work, but the track should not be considered closed until Phase 3 Green and Phase 4 UI verification are complete.

## 5. Recommended next actions

1. Land Phase 3 Green: either delete `convex/pipelines.ts` and migrate callers to `convex/pipelineRuns.ts`, or rewrite the placeholder functions to delegate to real `pipelineRuns` handlers.
2. Add frontend page/hook tests in Phase 4 to satisfy AC 8.
3. Update `metadata.json` to mark S1 `completed`, S2 `completed`, S3 `in_progress`, and S4 `pending`.
4. Run the targeted pivot/frontend test commands listed in `spec.md` and update `graph.db` before closing the track.
